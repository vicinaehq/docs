# Icons

Vicinae provides a built-in icon set that can be used in lists, actions, and other components via the `Icon` enum.

## Usage

```tsx
import { Icon, List } from '@vicinae/api';

<List.Item title="Settings" icon={Icon.Gear} />
```

## Available icons

{/* TODO: Icon enum reference */}
