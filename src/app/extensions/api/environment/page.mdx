# Environment

The `environment` object provides information about the current runtime context, such as paths, extension metadata, and the active theme.

## Usage

```tsx
import { environment } from '@vicinae/api';

console.log(environment.extensionName);
console.log(environment.assetsPath);
```

## Properties

{/* TODO: Properties table */}
