# Commands

The Commands API allows extensions to programmatically launch other commands within Vicinae.

## launchCommand

```tsx
import { launchCommand } from '@vicinae/api';
```

{/* TODO: Document available functions and types */}
